@model IEnumerable<lagu.Entity.Genre>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
           Nama Genre
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nama_genre_)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_genre_ }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_genre_ }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_genre_ })
        </td>
    </tr>
}

</table>
